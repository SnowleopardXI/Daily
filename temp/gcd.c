#include <stdio.h>

int gcd(int m, int n)
{
    int i, min = m; 
    if (m > n)
        ;
    for (i = min; i > 0; i--)
    {
        if (     )
            return     ;
    }
}
void main()
{
    int a, b, c;
    scanf("%d%d", &a, &b);
    c = ; 
    printf("两个数的最大公约数为: %d\n", c);
    printf("两个数的最小公倍数为: %d\n",     【5】 ); 
}
