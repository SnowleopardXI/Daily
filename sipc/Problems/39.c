#include <stdio.h>  
int main()
{
	int times,count,i=0,j,k,tmp;
	scanf("%d",&count);
	while (i<times)
	{
		for (j = 0; j < count; j++)
		{
			for (k = 0; k < count; k++)
			{
				scanf("%d",&tmp);
			}
			
		}
		
		i++;
	}
	
    return 0;  
}